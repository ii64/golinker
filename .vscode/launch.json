{
    "configurations": [
        {
            "name": "Debug main",
            "type": "go",
            "request": "launch",
            "mode": "exec",
            // "program": "${fileDirname}/${fileBasenameNoExtension}",
            "program": "${workspaceFolder}/main",
            "showRegisters": true,
            "showGlobalVariables": true,
            "dlvFlags": [
                // "--log",
                // "--log-output", "dap,rpc"
            ]
        },
        
    ]
}